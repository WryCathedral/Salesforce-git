name: SF upload Code Analyzer artifact
inputs:
  artifact_name:
    required: true

runs:
  using: composite
  steps:
    - uses: actions/upload-artifact@v6
      with:
        name: ${{ inputs.artifact_name }}
        path: reports/
        if-no-files-found: warn
